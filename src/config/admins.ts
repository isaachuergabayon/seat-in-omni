// Lista de logins de SharePoint con acceso al panel de administración.
// Para añadir un admin: añade su login (la parte antes de @ del email de Inditex).
// Tras editar este fichero hay que hacer rebuild y resubir a SharePoint.

export const ADMIN_USERS = [
  'isaachb',
]
